#include <iostream>
#include <string>

int main(void) {
  string s;
  int k;
  getline(cin, s);
  cin >> k;
  string k_st[k];
  for(int cur = 0; cur < s.length(); cur++) {
    for(int len = 1; cur + len <= s.length(); len++) {
      
    }
  }
  return 0;
}
